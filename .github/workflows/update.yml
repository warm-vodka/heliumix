name: Update Helium Browser

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: Check for Helium updates
        id: check_update
        run: |
          CURRENT=$(grep -oP 'version = "\K[^"]+' helium.nix)

          LATEST=$(curl -s --fail https://api.github.com/repos/imputnet/helium-linux/releases/latest | jq -r '.tag_name // empty') || LATEST=""
          LATEST=$(echo "$LATEST" | sed 's/^v//')

          echo "current=$CURRENT" >> "$GITHUB_OUTPUT"
          echo "latest=$LATEST" >> "$GITHUB_OUTPUT"

          if [ "$CURRENT" != "$LATEST" ] && [ -n "$LATEST" ]; then
            echo "update_needed=true" >> "$GITHUB_OUTPUT"
          else
            echo "update_needed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Update version in helium.nix
        if: steps.check_update.outputs.update_needed == 'true'
        run: |
          sed -i "s/version = \"[^\"]*\"/version = \"${{ steps.check_update.outputs.latest }}\"/" helium.nix

      - name: Calculate new hashes
        if: steps.check_update.outputs.update_needed == 'true'
        id: hash
        run: |
          VERSION="${{ steps.check_update.outputs.latest }}"
          AMD64_HASH=$(nix store prefetch-file --json "https://github.com/imputnet/helium-linux/releases/download/${VERSION}/helium-bin_${VERSION}-1_amd64.deb" | jq -r .hash)
          ARM64_HASH=$(nix store prefetch-file --json "https://github.com/imputnet/helium-linux/releases/download/${VERSION}/helium-bin_${VERSION}-1_arm64.deb" | jq -r .hash)
          echo "amd64=$AMD64_HASH" >> "$GITHUB_OUTPUT"
          echo "arm64=$ARM64_HASH" >> "$GITHUB_OUTPUT"

      - name: Update hashes in helium.nix
        if: steps.check_update.outputs.update_needed == 'true'
        run: |
          sed -i 's|x86_64-linux = "sha256-[^"]*"|x86_64-linux = "${{ steps.hash.outputs.amd64 }}"|' helium.nix
          sed -i 's|aarch64-linux = "sha256-[^"]*"|aarch64-linux = "${{ steps.hash.outputs.arm64 }}"|' helium.nix

      - name: Test build
        if: steps.check_update.outputs.update_needed == 'true'
        run: |
          nix build .#helium .#helium-wv

      - name: Commit and push changes
        if: steps.check_update.outputs.update_needed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout -B main origin/main
          git add helium.nix
          git commit -m "chore: update Helium to ${{ steps.check_update.outputs.latest }}" || echo "No changes to commit"
          git push origin main
